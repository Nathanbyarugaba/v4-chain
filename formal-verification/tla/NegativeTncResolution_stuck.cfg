\* One-sided / illiquid market: fewer offsetting quantums at overlapping
\* bankruptcy prices than the bankrupt position size, and (per the code) no
\* terminal fallback for a negative-TNC subaccount. Expect ResolvedEventually
\* to FAIL -> the negative-TNC subaccount is PERMANENTLY unresolvable, which
\* (via WithdrawalGating re-arming every block) freezes the whole collateral
\* pool's withdrawals forever. This confirms F1's reachability.
SPECIFICATION Spec
CONSTANTS
    Position                = 3
    OffsetCapacity          = 1
    FinalSettlementResolves = FALSE
INVARIANTS
    TypeOK
    StuckImpliesNegTnc
PROPERTIES
    ResolvedEventually
