\* Validated limiter: baseline > 0 (LimitParams.Validate enforces
\* baseline_minimum > 0 and baseline_tvl_ppm > 0). A depleted capacity recovers
\* to baseline, so a withdrawal up to baseline eventually succeeds.
\* Expect CanWithdrawEventually to HOLD.
SPECIFICATION Spec
CONSTANTS
    Baseline       = 3
    RecoverStep    = 1
    WithdrawAmount = 3
INVARIANTS
    TypeOK
PROPERTIES
    CanWithdrawEventually
